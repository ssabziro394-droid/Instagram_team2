# Техническое задание: Амирджон (Amirjon) — Team Lead

## Роль и задачи
Как Team Lead, ты отвечаешь за фундамент проекта, настройку архитектуры, а также за функционал Главной страницы (Ленты) и создание постов/Reels. Ты координируешь работу команды и проводишь код-ревью.

## Детальный план работ

### 1. Архитектура и Базовая настройка
- [ ] Инициализация проекта на Next.js (App Router).
- [ ] Настройка Tailwind CSS (глобальные стили, темы, цвета).
- [ ] Настройка Redux Toolkit (`store.ts`) и базового API клиента с помощью RTK Query (определение `baseUrl`).
- [ ] Создание глобального UI:
  - Sidebar (боковая панель для десктопа).
  - Bottom Navigation (нижняя панель для мобилок).
- [ ] Настройка линтеров (ESLint, Prettier) и правил форматирования кода для всей команды.
- [ ] Определение глобальных TypeScript-типов (`User`, `Post`, `Comment`).

### 2. Главная страница (Лента новостей / Home Feed)
- [ ] Разработка компонента `PostCard`:
  - Шапка (Аватарка пользователя, Username, кнопка "Опции").
  - Медиа-контент (Фото/Карусель).
  - Панель действий (Лайк, Комментарий, Поделиться, Сохранить).
  - Блок описания (Количество лайков, имя + текст поста, просмотр всех комментариев).
- [ ] Интеграция RTK Query (`getFeed`) для получения постов.
- [ ] Реализация локального стейта и optimistic updates для лайков и сохранения постов (чтобы UI реагировал мгновенно до ответа сервера).

### 3. Модуль создания контента (Create Post / Reels)
- [ ] Разработка модального окна или отдельной страницы "Создать".
- [ ] Реализация интерфейса загрузки медиа:
  - Drag-and-drop зона.
  - Предварительный просмотр выбранного изображения/видео.
- [ ] Форма для ввода описания (Caption), добавления локации (опционально) и хэштегов.
- [ ] Создание RTK Query мутаций (`createPost`, `createReel`) и логика загрузки файла (FormData) на сервер/MockAPI.

### 4. Обязанности Team Lead
- [ ] Контроль качества кода (Code Review) для PR от Идибоя, Ёсина и Исмоила.
- [ ] Помощь команде в разрешении конфликтов при слиянии (merge conflicts) и сложных моментах с RTK Query.
